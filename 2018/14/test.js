// eslint-disable-next-line no-unused-vars
const { findScore, testa2 } = require('./receipes');

describe('it should solve day 14', () => {
  it('doesnt work', () => {});
  /* describe('it should solve part 1', () => {
    it('it should get correct answer after 5 steps', () => {
      expect(findScore(37, 5, true)).toEqual('0124515891');
    });

    it('it should get correct answer after 1123 steps', () => {
      expect(testa2()).toEqual('0124515891');
    });

    it('it should get correct answer after 9 steps', () => {
      expect(findScore(37, 9, true)).toEqual('5158916779');
    });

    it('it should get correct answer after 18 steps', () => {
      expect(findScore(37, 18, true)).toEqual('9251071085');
    });

    it('it should get correct answer after 2018 steps', () => {
      expect(findScore(37, 2018, true)).toEqual('5941429882');
    });
  });
 */
  /*  describe('it should solve part 2', () => {
      it('should solve the test case', () => {
        expect().toEqual(325);
      });
    }); */
});
